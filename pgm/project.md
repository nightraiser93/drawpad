---
key: DRAW
name: Drawpad
status: active
# jira_base: https://your-domain.atlassian.net   # optional — makes Jira badges in the monitor clickable
---
# Drawpad

React Native sketching/note-taking app that hosts a local URL so any device
on the same wifi can view sketches live in a browser as they're drawn.

- **Key:** `DRAW` — every ticket is `DRAW-#####`.
- **Board + workflow:** [`README.md`](README.md) · rules in [`CLAUDE.md`](CLAUDE.md).

This file is the per-project descriptor the cross-project monitor reads (name, key, status).
